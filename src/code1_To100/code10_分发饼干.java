package code1_To100;

import java.util.Arrays;

/**
 * @author ajie
 * @date 2023/3/9
 * @description: https://leetcode.cn/problems/assign-cookies/description/
 */
public class code10_分发饼干 {
    public int findContentChildren(int[] g, int[] s) {
        Arrays.sort(g);
        Arrays.sort(s);
        int count = 0;
        int i = 0, j = 0;
        while (i < g.length && j < s.length) {

        }
        return 0;
    }
}
